# Luganda AI — Session Handoff (2026-06-06)

> Pick up here in a new window. Read this + `docs/ROADMAP_2026-06.md` and you're current.

---

## TL;DR
Direction is set: **data/model layer is the product, the app is the thin wedge.** Phase 0
is done and green. Next real build is **Phase 1 — the harvest agent** (or the keyboard web
version, user's call).

---

## What happened this session
1. **Brainstormed direction** — concluded "Luganda AI" is really two businesses (app vs
   data/model layer). Confirmed thesis: **data layer first, app as wedge.** Defensibility
   order: API/model > dataset > app.
2. **Audited the real repo** — the engine is fully built (translate pipeline, Whisper STT,
   MMS TTS, search, teach, chat, feedback, admin, fine-tune + HF-upload scripts, live on
   Railway). **The data is nearly empty** — that's the whole gap and the whole moat.
3. **Wrote the roadmap** → `docs/ROADMAP_2026-06.md` (Phases 0–5).
4. **Phase 0 executed and green:**
   - Built `scripts/corpus_status.py` (baseline + growth tracker, stdlib only, `--json` flag).
   - Confirmed live API healthy: `https://lugandastudio.com/api/v1/health` → ok.
   - Frontend `API_BASE = https://lugandastudio.com/api/v1`.
5. **Keyboard idea** — reframed it as the correction flywheel disguised as a product. Built
   an interactive mockup. Recommended: ship a **web writing-assistant** first, mobile IME later.

---

## Phase 0 baseline numbers (from `corpus_status.py`)
| Metric | Value |
|---|---|
| Audio recordings | 8 files, **0.5 min total** (basically empty) |
| Human-confirmed transcriptions | **0** |
| Training pairs | 47 |
| Correction pairs | **47 / 500** needed for LoRA |
| Feedback entries | 173 |
| Source datasets | vocab 424, sentences 110, proverbs 60, grammar 28 rows |
| ChromaDB | 6.3 MB | 
| Knowledge PDFs | 0 |

Re-run anytime: `python scripts/corpus_status.py`

---

## Open items
- [ ] **USER, on Windows:** activate TTS →
      `cd D:\projects\Luganda_AI_Studio && venv\Scripts\activate && pip install transformers scipy`
- [ ] **USER, on Windows:** run `python C:\Users\patri\OneDrive\MukalaziKnowledgeBank\scripts\compile_hermes_context.py`
      to sync today's memory into Hermes.
- [ ] **DECISION:** what to build next (see below).

---

## Next step — pick one (Claude's vote: #1)
1. **Phase 1 — harvest agent.** Build the nightly scrape → transcribe (Whisper) →
   flag-low-confidence-for-review → ingest loop, wired as a scheduled task. This is the
   engine that fills the empty corpus. *Nothing else matters until data flows.*
2. **Keyboard web version** (Phase 2). Textarea on lugandastudio.com with prediction bar +
   spell-check, reusing the existing API. Ships fast, starts collecting corrections.
3. **Cross-project routines audit** — sweep Sacco, Business Yoo, Farm Beacon for scheduled-task
   opportunities (user requested; still pending).

---

## Standing rules (do not forget)
- **Always remind Mukalazi to turn recurring workflows into skills.** Current skill candidates:
  `corpus-harvest`, `auto-transcribe-review`, `finetune-burst`, `corpus-status`.
- Keep costs ~$0. RunPod OFF except short fine-tune bursts. Phased rollout, not big-bang.
- User prefers simple explanations + real action plans over theory.

---

## Key paths
- Roadmap: `D:\projects\Luganda_AI_Studio\docs\ROADMAP_2026-06.md`
- Status tool: `D:\projects\Luganda_AI_Studio\scripts\corpus_status.py`
- Live API: `https://lugandastudio.com/api/v1`
- Memory updated: `project_luganda_ai.md`, `feedback_workflows_to_skills.md`
